{
  "name": "canmou-monorepo",
  "private": true,
  "description": "智汇参谋 - 企业智能助手",
  "scripts": {
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app",
    "quality:report": "node scripts/quality-report.mjs",
    "biz:hierarchy": "node scripts/query-biz-hierarchy.mjs",
    "collection:receivables": "node scripts/query-collection-receivables.mjs",
    "build:exe": "npm run build:exe --prefix app",
    "tauri": "npm run tauri --prefix app --",
    "tauri:dev": "npm run tauri:dev --prefix app",
    "tauri:build": "npm run tauri:build --prefix app"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  },
  "dependencies": {
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0"
  }
}
