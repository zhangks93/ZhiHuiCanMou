# Agent Terminal Guidelines

## Description
- This file is the stable rule set distilled from repeated issues in `agent-issue-log.md`.
- Record long-term mandatory precautions distilled from resolved issue patterns.
- Update this file when `agent-issue-log.md` reaches 50 entries, or earlier when a repeated issue clearly yields a reusable rule.
- Each guideline should be concise, reusable, easy to apply, and written as an explicit instruction.
- Do not log one-off incidents, task-specific context, or raw timelines here; keep concrete issue facts in `agent-issue-log.md`.
- Keep the table empty when no new guidelines are added.
- Use datetime format `YYYY-MM-DD HH:MM` (24h).

## Mandatory Action
- MUST: When issue facts are promoted from `agent-issue-log.md`, keep only stable and reusable rules here, and periodically compress overlapping items into simpler guidelines.

## Registration Table
| Time | Guideline |
| --- | --- |
|  |  |
